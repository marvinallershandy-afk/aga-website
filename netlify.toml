# Netlify-Config für SV Agathenburg-Dollern (v12).
[build]
  command = "npm run build"
  publish = "dist"

# SPA-Fallback: alle Routen auf die (prerenderte) index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
